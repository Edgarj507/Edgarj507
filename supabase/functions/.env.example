# Server-only secrets for edge functions. Set in production with:
#   supabase secrets set ALLOWED_ORIGINS=...
# SUPABASE_URL, SUPABASE_ANON_KEY and SUPABASE_SERVICE_ROLE_KEY are injected by the platform.
ALLOWED_ORIGINS=https://exclusive.golf,https://app.exclusive.golf,capacitor://localhost,https://localhost
